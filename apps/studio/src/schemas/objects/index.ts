import { customImageType, customOptionalImageType } from '@/schemas/objects/customImageType';
import { seoType } from '@/schemas/objects/seoType';

const schemaObjects = [customImageType, customOptionalImageType, seoType];

export default schemaObjects;
